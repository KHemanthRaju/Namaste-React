//import { useSelector } from "react-redux";

const Cart = () => {
  //const cartItems = useSelector((store) => store.cart.items);
  return (
    <div>
      <h2 className="font-bold text-3xl">Cart Items</h2>
    </div>
  );
};

export default Cart;
